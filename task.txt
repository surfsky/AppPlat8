----------------------------------------------------------
数据
----------------------------------------------------------

# 工程

- 修正调试配置（launch.json），debug consoler 是没问题的，另外两个都有问题；
- 编写建设方案


# 隐患排查业务


*** 任务和事件：
    任务
        所有检查都有一个任务ID，默认为日常任务；
        这样便于统计某个检查归属在哪个专项任务下面；
        任务可以设置起止时间、受理部门；
        通用任务以及业务表单的融合
    任务和事件
        任务派发和分配、进度统计、巡查可选任务
        事件独立，不绑定对象，可关联对象
    督查整改模块：
        督察员检查到的隐患，要求责任单位人员进行整改。
        隐患需有字段：检查员、检查类别（如日常巡查、督查、专项检查等）、责任人员就是对象的管理人员
        我的隐患处理任务：责任人是我的隐患
关于风险点
    方案一：增加风险点 Check 对象，用于保存风险点，可关联到对象，也可以独立存在，并关联到企业责任人、政府监管人
        CheckRisk: Id, CheckObjectId, RiskLevel, Pic, Name, Desc
    方案二：直接在 CheckObject 用tag标注，风险信息用附件存储
        先实现 SearchObject(....Tag....) 方法，可根据标签搜索对象，风险点文档先放在对象附件里面


- 隐患排查
- 统计及调度
- web监管进度
- 做简单的统计报表

我的任务 me/tasks 任务看板
    左中右三块：待办、处理中、已完成
    可增加、修改或拖动

检查对象CheckObjects 页面的过滤条件：
CheckSheet 中有两个特别大的检查表，预计要删除；
重写梳理新系统的检查表
/CheckObject 增加安全责任人（即CheckContact表，不用改），可以考虑
增加联系人类型枚举, 并修改完善list、form页面

# 后台调度进度（App.Consoler）
    用独立的sqlite数据库？
    看是否要用数据库进行配置任务，用反射方式填写任务需要调用的方法
    显示各项任务及其进度
        console 显示
        主项目显示
    Quiz.NET 有没有适配的UI框架
    任务依赖？
    日志

# 任务交办系统

- 任务交派流转和进度跟踪
- 进度视图：看板、甘特图、周任务图
- Canvas编辑引擎：LeaferJs



# 框架

--------------------------------------------------------
- UI框架
--------------------------------------------------------
更换时间范围控件

智能表格
    - Excel 编辑：可直接在页面编辑表格，可直接黏贴数据，
    - 完成通用数据维护界面（可考虑用UIConfignfig类），体模型类自动生成对应的列表和表单页面
    - ai问答表格，任意请求都能返回数据表格

EleUI
    - 所有页面都加上 Loading 效果；
    - 实现所有EleUI控件的亮暗双色模式（如 elemanger drawer 在驾驶舱使用暗色系）；先用json定义样式表 Theme{...}，另所有控件都支持 SetTheme(theme) 方法；
    - EleTable.Filter 增加展示方式属性：Tool（全部显示在工具区）、Drawer（显示过滤按钮，点击后用抽屉展示）、FirstRow（仅展示首行并显示过滤按钮，更多用drawer显示）
    - EleTable 实现列的数据跳转，给EleColumn增加属性 PopupUrl=""


文档预览技术方案备案：
    kkfileviewer, jitword, onlyoffice
    对于excel文件，尝试使用canvas-cells 打开编辑窗口；


表格：多选、批量修改能力（*）
数据模型的UI自动化展示和编辑（*）
    创建自动展示列表的页面 UISetting
    创建自动展示表单编辑的页面

工作流
    配置工作流程图
    用于oa文档流转

重构数据库
    Entity：Online 逻辑有点啰嗦，可参考 appplat4实现（容后）
    改用 postgresql, 可实现全文检索


其它
    实现网页水印（可找WebConfig 进行配置），参考https://element-plus.org/en-US/component/watermark
    删除重复的 bootstrap 目录(（容后， blazor两个地方用到了)
    把id从int改为long，再启用snowflakeId方案
        Log.Get(id) 返回null？1833214689845186600
            经测试是 sqlite 的问题，或者是ef的问题
            换回int自增的id，就没问题了



--------------------------------------------------------
业务
--------------------------------------------------------
任务交办系统及进度
    实现检查任务流程
        bug：标签表单，保存新标签后，并未刷新父节点下拉树。可以用新增的 OnChange 方法了
        隐患复查记录：隐患ID、记录人ID、日志时间、说明、图片列表、整改状态
    优化通用任务（AssignTask）
        - Id、OrgId、Type任务类别、TargetValue任务指标值、TargetValueUnit指标值单位、CurrentValue当前进度值、ParentId、DataJson扩展属性
        - 实现任务面板，分“全部”、“我的”、“我参与的”，可显示任务的进度，及任务的树状及任务分解情况
        - 某个事件的归属任务可多选，如一个企业检查可挂在多个任务下面（如日常检查、春季检查、XX专项检查等）

继续优化隐患闭环
    - 检查详情（只读）页面，可查看对应的隐患记录表
    - 完成隐患闭环操作
    - 看情况，若有必要把 CheckFlow 改为 CheckNew
    - 检查对应的任务可多选，这个排查既归属于这个任务，又归属于那个任务，有重叠的。

完善项目管理
    实现项目管理的附件功能
    实现项目管理的历史功能
    需补充数据模型：
        项目进度日志：ID、项目ID、操作人、说明、项目状态、进度百分比
        资产变更日志：ID、资产ID、执有人、说明（算了）

统计
    调度引擎：可用于后台自动调度生成统计报表
	对象类报表
		企业分类统计表（表四）
		工矿企业备查点汇总表（六）
		周期排查进度表（三）
	隐患类报表
		隐患排查闭环表（二）
	任务类报表
		工业企业排查进度统计表（一）
		基础专项任务进度统计表（四）
	个人工作量报表
    -----------------------------------
    展示框架
        用vtable展示
            透视表:https://visactor.com/vtable/demo/table-type/pivot-chart
            表内图表：
            表头分组（https://visactor.com/vtable/demo/basic-functionality/list-table-header-group-collapse）
        甘特图可做任务进度（交办）管理、日历图
            表格直接编辑：https://visactor.com/vtable/demo/edit/add-delete-records

定时任务
    每日定时统计数据
    每日到期提醒


--------------------------------------------------------
驾驶舱框架
--------------------------------------------------------
rtsp 监控查看方案：
    https://github.com/flyfish-dev/rtsp
        https://rtsp-roan.vercel.app[
        <rtsp-player
          url="rtsp://user:pass@192.168.1.64:554/Streaming/Channels/101"
          width="960"
          height="540"
          autoplay
          controls>
        </rtsp-player>
        rtsp://9627b0bf2a7b.entrypoint.cloud.wowza.com:1935/app-p5260J38/66abe4b9_stream1

前端文档查看方案：
    FlyfishFileViewer：


图片变三维模型：
    https://github.com/huangserva/3DCellForge

优先
    实现index/gis 点位面板范围检索，在点位图层中增加过滤条件：几公里、名称
    实现接口方式获取点位数据
    实现excel上传后的数据管理页面，可检索排序，可CRUD
    3、事发点标定、范围搜索及ai统计
    5、语音或文本命令ai操作
    6、事件记录及处置流程，含预案过程和复盘
    7、人员定位和上图
    8、酷炫动画

gismenu 支持几种图层及融合
    /点位（geometry）：数据库取数
    接口（api）：
        数据直接取自接口，接口取数成功可获取数量、最后取数时间，没有就提示取数失败；
        实现检索条件，如SearchJson（可用dict编辑器）
        * 实现检查对象Gis接口（如tag：人员密集场所、学校、娱乐场所），接口字段参考GisGeometry
        补充接口调度日志，融合到系统的log表，增加日志类别api
        gis菜单项右侧，显示一个刷新按钮，点击后可手动刷新接口数据
        可定时刷新
    脚本（script）：
        点击后会执行固定接口的脚本（参考Doc/Map/Core/MapLayer），显示相关图层，并自主定时刷新；如云图、
        用枚举管理脚本类别，在后台可配置Scene使用哪些脚本
        支持定时刷新
    事件（realtime）：反向推送告警
        - 事件也作为为图层，gisapi需要定义抽取周期、需增加订阅能力。虚拟一个数据源接口
        - 应急人员/移动设备都可以作为图层
        - 人员/车辆/移动设备也是图层，手机端打开定位就会刷新在地图上
        事件点
            是否直接融合到 GisGeometry 增加一个来源类别即可，事件本质上也是一种点位，便于统一数据结构；
            创建数据表 GisEvent（id、title、addr、gps、createdt、expiredt、status，appid， from）
            创建后台管理页面 gis/events，eventForm
            创建接口 
                apis/gisevent/list获取事件清单，get获取事件详情，create新事件，update更新事件，finish结束事件
                公开接口，外部应用可调用接口，用app token或者登录机制
            结合后台signalr，推送事件到前段页面并展示
            手动添加事件：
                在gis/index驾驶舱左上角增加“标注”按钮，点击后弹出下拉菜单（先显示一个“事件点”），点击后可在地图上标注点位，标注成功用elemanger drawer 滑出事件编辑窗口；
        如何融合展示 signalr 实时推送图层？
            创建一个 SignalR 测试hub，用于展示人员的位置
            给图层增加一个值班人员的图层
            点击该图层后，根据signalr实时数据展示人员
            创建signalr服务，提供频道，客户端收到后会刷新相应菜单图层或面板
        实现消息面板
            - 平时是横幅状态，可点击展示事件面板
            - 事件分类别
            - 先实现台风预警
            - 定义统一事件通知接口，可接受各种异源事件接口
            告警消息实时推送：signalr


统一固定点位 GisGeometry 和接口 GisApi 获取的点位
    /所有都是图层，gismenu真的要改成gislayer了（不改了，事实上一个menu可以容纳多个layer）
    - 改造方案一（推荐）
        数据都从gisgeometry取，接口根据配置在后台自动刷新数据；
        增加ApiId字段，表示数据是中Api中获取的；
        Gis数据清单面板，就可以以普通表格方式进行展示和检索；
        提供一个刷新数据按钮，从接口取数据并插入数据库，完毕后刷新页面；
        优点：任何时候都有数据可以展示，统一展示方式，统一检索方式；
        缺点：该方案会导致数据不实时，导致疑惑和解释不清楚、也会造成数据冗余，如检查对象（CheckObjec本身就提供物理表了，根据此逻辑需要再复制到GisGeometry表）
    - 改造方案二：
        gislayer增加dataurl参数，如果不为空，则直接从接口取数（含分页逻辑）
        如果接口挂了，图层菜单节点提示错误且图层变红；
        增加字段Autosave，若为true可保存到gisgemetry
        允许用户从gisgeometry表取历史数据。
    - 如何展示点位离线和紧急状态：
        GisGeometry增加 Status 属性（Normal、Offline、Ergent），默认为Normal，图标分别为灰色和红色闪烁；图标要能用代码变化色彩（考虑用cssfilter或者iconfont）
    - 增加 ApiLog 表
        用于记录Api刷新记录，字段为：Id、FetchDt，DataCnt，IsSucceed
        每次刷新记录时记录一下


路径数据如何保存和显示？
    - 方案一：弄个点位轨迹表GeometryLog，字段geometryid、logDt、action, IsOnline。如台风路径，一个台风一个点位。同时显示会很乱？那给gislayer加一个属性，showDays，表示只显示最近几天的数据。另外，数据列表面板可以多选；搞个通用方法和ui在地图上展示轨迹；
    - 方案二：路径可保存到datajson（否定,不好检索和动态更新）。也可保存到各自的业务表中，用pageurl参数控制显示展示页面；



增加资源图层
    可用api接口方式获取数据，也可以随便标个点位，上传附件即可；
    联系人：用excel导入数据，可查询；
    预案：
        作为对象附件，word、excel、脑图、maimaid流程图文件
        作为固定表的列存储（很难规范）
        作为结构化存储，要定义一个合适的json结构，用于描述事件预案操作流程（含各阶段任务、责任人等）
    *** 尽快实现通用版本excel导入后编辑查询能力

还得重构gis/index中用到的各个js模块
    现有的模式感觉还是有点不舒服，还是想改成class；
    用typescript重构前端，也便于实时代码合并混淆和安全，避免单独文件被三方提取使用

其它
    实现gis/roleGisMenu管理页面，授权角色可以管理哪些gis菜单，否则也要用老的方式配置每个菜单的power收取
    接入雪亮工程监控

重构面板和报表（容后）
    - 实现Stat/Chart管理页面
        （对应实体类 StatChart，用于定义统计图表
        也可以根据dataurl（或ApiUrl）及pageurl参数与具体数据对接起来，避免只做一个无意义的花架子
        可用echarts展示（有明暗两种风格或可定制风格）
    - 修改gispanel，一个panel可容纳多个图表（用模板的方式容纳多个chart）
    GisGeometry 可导入excel，便于快速录入数据
    Gis数据多部门协同编辑: 仅菜单的管理员（ownerid）才能编辑该数据
    驾驶舱：暗黑/亮白两种模式
    - 驾驶舱融合ai问答能力，根据请求自动选取相应的图层和数据并展示；


Gis：色斑图

--------------------------------------------------------
后台框架
--------------------------------------------------------
全文检索/模糊查询方案
    like性能不足
    用ElasticSearch 
    用ev向量

文档展示方案
    实现在线excel附件编辑功能，简化系统业务表堆积压力，大部分的数据都用excel管理就行了；要有历史留档能力，确保数据安全；
    open file viewer：
        https://github.com/xushanpei/open-file-viewer
        经测试，对word文件的支持较好，可考虑用该方案解析部分文件。其它文件如脑图、三维模型支持不完善；

知识库及授权规划
    知识库采用自由上传文件方式（云文档）
        目录，文件都自由创建和上传
        安全防护：网站禁止访问运行
    详讲《政务内部私有化知识库项目》
    在线预览及编辑
    全文检索、AI矢量检索
    授权
        复杂的授权：公开，本组织内，指定科室等（不推荐）
        简单的授权：默认只能查看自己及下属科室的文档（推荐）
        文档级别限制：用户查阅等级 >= 文档等级
        文档分享：分享给用户、限制查看编辑权限

原有的用菜单权限管理逻辑需要重构
    思考：
        现有的菜单控制不直观，计划创建一个页面 RoleMenus，左侧是角色，右侧是菜单树供勾选；
        每个页面的权限都在代码中已经用Auth、AllowPower设置了，和菜单权限有重复的嫌疑
        实现角色菜单管理页面，原有的用菜单权限管理逻辑，过于基础，不能直观的体现某个用户（角色）能访问的菜单；
        如何防止用户直接打网址？页面已有权限限制了。如果启用角色菜单方案，又担心不一致的情况；
    改进策略：
        菜单权限从页面权限 attribute 自动获取，不用手动设置，避免重复设置或产生冲突
        实现角色菜单只读页面，供核查用
    创建运维工具：检索所有pages/apis下的页面，看授权是否有，是否正确


信创适配数据库
    sqlite扩展
        数据库sqllite替代：turso （国外）：不知道支持不支持EF。
        尝试用jsonb来存储只读的复杂数据
            只有 PostgreSQL 才有 jsonb 原生二进制 JSON 类型；其他数据库没有 jsonb，只有各自的 JSON 专用类型（MySQL json、SQL Server json、Oracle JSON、SQLite JSON1）。
            EF Core 7+ 统一提供 .ToJson() 强类型 POCO 映射，所有主流数据库驱动都支持，但底层字段名各不相同。
        使用ev来模糊查询；
    人大金仓 KingbaseES（V8/V9，政务首选 PG 兼容库）
        官网首页：https://www.kingbase.com.cn/
        专用下载中心直达：https://www.kingbase.com.cn/Download.html
        下载要求：邮箱验证，免费 90 天试用授权，支持飞腾 / 鲲鹏 / 龙芯 / 海光、麒麟 / 统信。
    达梦 DM8（自研内核，支持 PG 兼容模式）
        官网首页：https://www.dameng.com/
        在线服务平台（开发版下载）：https://eco.dameng.com/download/
        包含：DM8 服务端、DM 嵌入式单文件版，分 x86 / 信创 ARM / 龙芯架构。
    openGauss：
        官方下载页：https://opengauss.org/zh/download/
        开源镜像：https://github.com/opengauss-mirror/openGauss-server
        完全免费开源，无授权限制，适配鲲鹏、openEuler。
    星瑞格 SinoDB Lite（对标 SQLite 国产单文件库）
        厂商官网：http://www.sinoregal.cn/
        社区试用下载专区：https://forum.sinoregal.cn/t/topic/56
        社区主站：https://forum.sinoregal.cn/，注册后下载 Lite 单文件版。
    达梦 DM Embedded（单.dmb 文件嵌入式内核）
        下载入口：https://eco.dameng.com/download/
        下载时选择嵌入式开发包，区别于服务端 ISO 安装包。

计划统一token和cookie的内容，只是存储位置不一样（cookie、header），内容应该是一样的；
    cookie和token的合并
    把 token 改为用 header authorization bearer 方式，更为现代和通用（并行吧，否则要大改前端页面，每个ajax操作都要修改header部分）
    网页端都要改了，原先会自动带上cookie，现在可考虑存储在localstorage里面，并修改vue app，每个请求都要在header中带上参数




--------------------------------------------------------
AI 融合
--------------------------------------------------------
AI 对软件的重构
    AI来引导用户，而不是用户去用软件；
    根据用户需求，调用cli或者api服务，自动生成交互界面或根据问答，自动展示结果
    数据挖掘决策

AI聊天
    /普通对话
    可上传图片分析（用deepseek 新版接口、vlc）
    数据库 MCP 可访问能力
        实现AI聊天检索数据库及统计的能力
        提供独立的AIData页面（或扩展AIChat页面），每次AI对话查询，都会显示数据表格（含下载能力，需约束AI可访问的数据表和列）
        需限制AI能访问的表格和字段，并结合当前提问人的授权安全级别、授权数据范围

系统服务能力
    /实现api接口服务
    实现系统cli功能
    实现系统MCP功能，可以对外提供服务，如气象、企业等查询操作能力
    实现驾驶舱AI互动操作能力：
        切换场景
        新建事件（内容地点）
        显示周边资源
        列出图层清单
        显示指定图层

AI辅助
    cad转三维模型：
    生成图片、音乐、视频等



--------------------------------------------------------
更多
--------------------------------------------------------
新开项目pro收费版本git仓库
部署codex开发环境，copilot太贵了

# 安全考虑
    - 接入移动能力平台短信接口（避免安全隐患）
    - 接入阿里短信平台短信接口
    - Api接口列表页面必须授权才能访问

部署
    用笔记本？局内政务网，可使用公网，需部署防火墙只运行局内网段访问
    用服务器？现在只开公网，以后迁移到内网，需部署防火墙
    部署：waf 参考“雷池”，或部署三方防火墙，仅授权网段访问：10.36.247.1-240

技术思路
    系统易用性和扩展性
        Excel 表格编辑及分享（参考云表）：上传一个excel即可获得页面检索、列表、表单编辑、excel编辑能力
        可ai对话的数据库管理工具/网站
    完全的接口、前端、测试的剥离



--------------------------------------------------------
Done
--------------------------------------------------------
    优化检查对象
        批量给数据打标签
        批量修改能力：多选后点击编辑，弹出批量修改窗口；其中
            优化EleForm，可批量编辑，可指定编辑字段，如 CheckObjectForm?id=xxx,xxx,xxx&field=xxx,xxxx,xxx ，采纳
            在CheckObjects页面，增加“批量失效”工具栏按钮
        对象标志编码（3303830000000535405），温州答复。我们需要创建一个编码方法，并给org设置编码
    弄个页面，导入各种表格，根据AI对话，梳理出一个展示页面，并固化展示方式；

    gis/index
        融合搜索：支持地图服务搜索、系统点位搜索
        实现gismenu 实时数据
            - gismenu设置一个signalr 的hub地址就行
            - gis/index实现对signalr的支持
        UI优化
            gis/index 顶部实现流光动画效果
            gis/index 页面遮住左下角的“mapbox”标签
        可接入其它地图层（如高德地图），也用MapLayer接口实现，点击后切换地图服务源；
        Gis/index maplayer 图层增加设置面板，放到右侧
        地图顶部（或底部）显示滚动告警；

    优化
        使用本地AI服务
        改用snowflakeid
        优化HttpApi，增加AuthLogin全局配置（默认为true，表示所有接口默认都需要登陆后才能使用），若与具体API冲突，以具体API为准；
        角色和 ContactMenu 管理（RoleContactMenu）。

    其它驾驶舱必须的内容
        把预算表导入数字化系统
        扩展 GisPanel
            原面板是支持Content、ChartJson两种方式来显示内容，其中 ChartJson方式已经能够根据json或者api来显示图表；
            但Content只能显示静态内容，也想让其支持根据接口+页面模版组合生成内容，如想生成一个值日表面板；
            实现示例：
                将“应急资源”（ID=1）的面板的图表设置改为通过接口方式获取，写一个对应的HttpApi来提供数据（/httpapi/Stat/EmergencyResouces），并测试通过
                新增“值班表”面板，写API（接口如：/httpapi/OA/Dutys?day=20260711），配置Content，并测试通过；
            增加自动刷新逻辑，增加字段Cron（参考CRON表达式），在客户端会根据该表达式自动刷新面板（最好找一个cron的js类库，不要自己写）；
        值班信息（Duty）
            实现值班信息管理，字段：Day、OrgId、带班领导、值班长、值班员1、值班员2、值班员3、值班员4、值班员5、值班员6、值班员7、值班员8；
            实现相关数据接口
            用GisPanel实现：显示本日值班人信息，可切换日期；
    AI
        把语音操作弄起来：
            切换到森林防火场景（模式）
            切换到
            查看新城产业园所有企业
            显示基础科本年度的费用预算
            结构化输出：以支持mcp调用和分析，可用以隐患识别格式化输出；
        角色形象模拟
            抽象科技生物模拟，不用人形，用一团光芒黑洞也行；
            人形模拟，模仿生化危机”Alice“，可通过监控感知你的位置来调整视角；
        AI能力
            img2three 将图片转化为三维
            geoai 地图变化检测
            lingbot 实时三维建模
    优化
        切换场景时，先flyto到新场景的中心坐标，fly结束后再更改地图底图、投影方式、三维地形、数据图层等；或者倒过来；
        把所有的EleXXXX 控件名称改为 EXXX，简化控件名称
        BadSmell
            IGeometry.RawId, ItemUrl
            GisMenu.ItemUrl -> DataItemUrl
    优化eletable 的 枚举显示方式，自动计算颜色值（绿色-》红色）


    检查对象
        针对花名册，给企业打标签，补缺补漏
        化工及危化企业怎么数据不对

    检查表
        配置八大风险检查表（****董局）
        隐患查找和统计

    换几个统计面板，和企业统计、应急力量统计
    性能文笔
        迁移数据库到postgresql
            id 要改为雪花id
            数据拷贝到新数据库
            用以迁移到国产数据库，如（KingbaseDb）
            向量数据库如何迁移？
        实现页面Loading效果：
            统一的页面loading
            弄一个不依赖于Element的Loading.js 模块，可显示和隐藏全页面蒙版和loading；优于其它模块加载；
            有时候页面加载会白屏一会，不友好；
            感觉不是数据加载慢，而是element vue页面创建慢。能否页面vue结构渲染前就显示loading效果；找个页面做一下示例；
            检查一下 EleForm、EleTable 在每次加载数据时是否带了 loading 效果，如果没有就加上。（其实加上了，但是没用）
            这样有两层loading逻辑，第一次的页面创建，每次的数据加载，会优化用户体验；

    解决性能问题：http://172.20.165.221:6060/httpapi/orgs/GetAuthOrgTree 
    实现权限管理
        知识库目录角色授权管理
        写个页面，列出所有页面需要的权限
    实现专项检查任务分派及认领逻辑
        专项检查任务创建、选择清单、认领和分配
        专项检查任务报表及清单
        隐患清单查询
            可根据 任务名称、检查项标签来过滤，以获得具体的清单
            检查项增加标签，可根据标签来进行分项统计
            检查时可选多个任务
        检查项合并及调整：做一张大的检查总表，可过滤查询：类别、领域、名称、编号
        日报统计

    知识库
        实现文件和目录管理
            实现递归检索
            实现目录和文件的混合展示
            实现多目录文件的批量上传加载
            实现大文件的上传和下载
        实现检索页面
            实现全文检索
            实现AI RAG增强检索
        测试瑞泽的权限，让他自己编辑
    参考
        也修正drawer 的拖拽调整宽度的bug：drawer向左拖动时问题不大，可以保持跟踪实时调整drawer的大小，但是往右拖动时经常会丢失跟踪，导致drawer无法调整宽度。另外拖动时也感觉不平滑，会抖动；
            我查了官方文档，只需要一个 resizable 参数就可以的，不知道我们现在的DrawerHelper时如何实现的，如果官方的实现方法没问题，就切换回官方的逻辑以简化代码：
            <el-drawer v-model="drawer" :direction="direction" resizable>
                This is drawer content.
            </el-drawer>
            word渲染丢失文字格式
            excel渲染丢失合并行逻辑
    把 AI 对话页面（AI/Chat）中的语音用 QWen3 ASR 0.6B 服务替代 
    实现会议导入：组织、日期、类别、图片、内容
    检查一下角色权限，为什么董局无法查看安全生成/管理对象页面
    组织表不能随便删除啊，要做成逻辑删除的


    知识库
        把基础科的 AI 知识库弄起来 RAG
        /针对一个目录，直接上传若干文件或压缩文件，后台生成rag 向量数据库
        AI RAG向量数据库
        传统方案：拆分文本分析写到向量数据库
        图像方案：pixcel rag，graphy rag
        文档保存目录要考虑一下，集中入口，或者迁移到OSS（预留）

    简单的扩展系统功能（也许就是XX表）
        上传一个excel
        可直接web编辑该excel
        也可以生成对应的列表和表单页面
        可设置过滤条件？保存到哪里？
        考察一下Notion软件

    Gis优化
        /修正gis/index 通过api接口访问数据的点位详情无法查看的bug
        实现数字孪生粗模编辑器、精模裁剪器
        接入雪亮工程监控
        点位编辑器页面性能很差，如何优化（注：配置和展示剥离是合理的，后台再怎么卡也不会影响前台展示，就是要注意一些代码要复用，也可以避免渲染逻辑不一致的情况）


2-26/09/24
    /梳理检查表，做好排序，完善高频隐患检查表
    /优化topbar，增加半透明svg背景图片
    隐患排查流程
        修正对象的隐患清单页面
        跑通隐患排查流程、复查、状态、
        流程及后台
            配置一个简单流畅：新建、复查、复查、复查、复查....归档
            创建网页，可启动控制台，并显示输出
        专项任务统计
            导出“一企一档”资料：一个企业一个word，可批量筛选企业后导出。该功能需要定制，预计工时4人天。
            定制统计表（老旧园区专项检查）
    对象管理
        简化141对象数据更新：
            步骤：导出141系统excel数据，导入本系统，合并规则及清单、批量更新数据
            合并规则
                两个系统的对象都有一个统一的编码，或有一个更新日期，
                从141系统导出对象清单excel后，导入本系统，若编码一致，且更新日期大于本地，则更新指定字段的数据；或本地服务器都更新了则由用户自己判断；
                标签是否合并分拆：拆分为141标签以及应急专用标签，更新方便，但是管理复杂；
    解决知识库索引页的排序问题
    权限控制优化
        重新梳理权限体系：创建简单的扩展方法，以约束查询，如 q.ApplyUserOrg(userId)，而不是全局进行限制，心里没底（切换角色后就担心无法使用）
        修改PageBase，默认要求AuthLogin，除非显式不要求检测
    报表
        修正报表导出excel格式；
        实现长时间查询报表返回（可用signal R技术）
    知识库
        本地知识库程序
        知识库: 知识库管理页面，右侧的部分需要定制，可同时显示目录和文件，操作方式和资源管理器一致；
    UI
        实现EleTable 左右布局能力，增加<Filter><SidePanel>....</SidePanlel>
    工作流
        实现 Feedback 处理流程，重新熟悉一下 liteflow 工作流引擎

2026/09/23
    /将AMap高德地图的key用数据库进行存储；剔除硬编码；
    /国密算法库
    /简化LoginAI 页面
    /手机号都屏蔽掉，避免泄漏

2026/09/22
    /修正预算页面 Amount -》 Fee，TypeName、OrgName
    /增加枚举色彩选项：检查对象》风险等级
    /驾驶舱地图框架要改
        /mapbox访问时好时不好，且受到国内政府项目审核的约束
        /先测试一下是mapbox框架不能访问，还是地图不能访问。
            /如果是前者那就更换地图框架，改用mapbtalks 或 maplibre
            /如果是后者则更改地图源头即可（支持天地图、openstreetmap、高德等(正确)
        /后期再考虑迁移到MapLibre框架（MapBox的开源框架）
        /新增地图支持
            /渲染openstreetmap
            /渲染天地图
        /增加地图设置配置 SiteConfig.MapboxKey, TiandituKey
        /优化地图场景，增加更多地图类型、IsDefault 属性


