## 关于数据接口API

接口
    检查业务数据接口
        /对象
        /检查表、检查项
        /标签
        /排查
        /隐患
        /检查任务
        /隐患处理
    OA相关接口
        知识库
        任务、指派事件、预算等（若无必要，直接用网页）
决策
    只保留必要的接口，其它的容后吧，暂时没有开放接口给三方app的需求
    若有了，再根据界面迁移数据接口，其实就是pagemodel交互时传输的数据

